<!-- お気に入りしているかどうかの条件式 -->
<div class='ms-auto'>
  <% if current_user.favorite?(post) %>
    <%= render 'unfavorite', { post: post} %>
  <% else %>
    <%= render 'favorite', { post: post} %>
  <% end %>
</div>