<!-- 検索候補 -->
<% if candidates.any? %>
  <ul class="search-candidates-list">
    <% candidates.each do |candidate| %>
      <li class="candidate-item"
          data-action="click->search#selectCandidate"
          data-search-value="<%= candidate %>">
        <%= highlight(candidate, params[:keyword]) %>
      </li>
    <% end %>
  </ul>
<% end %>