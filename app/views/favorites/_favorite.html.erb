<% if post.favorited_by?(current_user) %>
  <div id="favorite-link-<%= post.id %>" data-favorite-count="<%= post.favorites.count %>">
    ♥<span class="favorite-count"><%= post.favorites.count %></span>いいね
  </div>
<% else %>
  <div id="favorite-link-<%= post.id %>" data-favorite-count="<%= post.favorites.count %>">
    ♡<span class="favorite-count"><%= post.favorites.count %></span>いいね
  </div>
<% end %>